// ==== ------------------------------------------------------------------ ====
// === DO NOT EDIT: Generated by GenActors                     
// ==== ------------------------------------------------------------------ ====

//===----------------------------------------------------------------------===//
//
// This source file is part of the Swift Distributed Actors open source project
//
// Copyright (c) 2019-2020 Apple Inc. and the Swift Distributed Actors project authors
// Licensed under Apache License v2.0
//
// See LICENSE.txt for license information
// See CONTRIBUTORS.md for the list of Swift Distributed Actors project authors
//
// SPDX-License-Identifier: Apache-2.0
//
//===----------------------------------------------------------------------===//

@testable import DistributedActors
import DistributedActorsTestKit
import XCTest

// ==== ----------------------------------------------------------------------------------------------------------------
// MARK: DO NOT EDIT: Generated MessageRouter messages 

/// DO NOT EDIT: Generated MessageRouter messages
extension MessageRouter {

    public enum Message: ActorMessage { 
        case registerTarget(String, actor: AnyActor.MessageTarget) 
        case dispatchMessage(String, message: MessageType, replyTo: ActorRef<String>) 
    }
    
}
// ==== ----------------------------------------------------------------------------------------------------------------
// MARK: DO NOT EDIT: Generated MessageRouter behavior

extension MessageRouter {

    public static func makeBehavior(instance: MessageRouter) -> Behavior<Message> {
        return .setup { _context in
            let context = Actor<MessageRouter>.Context(underlying: _context)
            let instance = instance

            instance.preStart(context: context)

            return Behavior<Message>.receiveMessage { message in
                switch message { 
                
                case .registerTarget(let identifier, let actor):
                                        instance.registerTarget(identifier, actor: actor)

                     
                case .dispatchMessage(let identifier, let message, let replyTo):
                                        instance.dispatchMessage(identifier, message: message, replyTo: replyTo)

                     
                
                }
                return .same
            }.receiveSignal { _context, signal in 
                let context = Actor<MessageRouter>.Context(underlying: _context)

                switch signal {
                case is Signals.PostStop: 
                    instance.postStop(context: context)
                    return .same
                case let terminated as Signals.Terminated:
                    switch try instance.receiveTerminated(context: context, terminated: terminated) {
                    case .unhandled: 
                        return .unhandled
                    case .stop: 
                        return .stop
                    case .ignore: 
                        return .same
                    }
                default:
                    try instance.receiveSignal(context: context, signal: signal)
                    return .same
                }
            }
        }
    }
}
// ==== ----------------------------------------------------------------------------------------------------------------
// MARK: Extend Actor for MessageRouter

extension Actor where Act.Message == MessageRouter.Message {

     func registerTarget(_ identifier: String, actor: AnyActor.MessageTarget) {
        self.ref.tell(Self.Message.registerTarget(identifier, actor: actor))
    }
 

     func dispatchMessage(_ identifier: String, message: MessageType, replyTo: ActorRef<String>) {
        self.ref.tell(Self.Message.dispatchMessage(identifier, message: message, replyTo: replyTo))
    }
 

}
