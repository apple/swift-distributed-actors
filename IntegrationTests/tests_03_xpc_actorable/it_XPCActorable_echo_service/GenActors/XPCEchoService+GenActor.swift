// ==== ------------------------------------------------------------------ ====
// === DO NOT EDIT: Generated by GenActors                     
// ==== ------------------------------------------------------------------ ====

//===----------------------------------------------------------------------===//
//
// This source file is part of the Swift Distributed Actors open source project
//
// Copyright (c) 2019 Apple Inc. and the Swift Distributed Actors project authors
// Licensed under Apache License v2.0
//
// See LICENSE.txt for license information
// See CONTRIBUTORS.md for the list of Swift Distributed Actors project authors
//
// SPDX-License-Identifier: Apache-2.0
//
//===----------------------------------------------------------------------===//

import DistributedActors
import DistributedActorsXPC
import it_XPCActorable_echo_api
import Files

// ==== ----------------------------------------------------------------------------------------------------------------
// MARK: DO NOT EDIT: Generated XPCEchoService messages 

/// DO NOT EDIT: Generated XPCEchoService messages
extension XPCEchoService {

    public enum Message: ActorMessage { 
        case xPCEchoServiceProtocol(/*TODO: MODULE.*/GeneratedActor.Messages.XPCEchoServiceProtocol) 
    }
    
    /// Performs boxing of GeneratedActor.Messages.XPCEchoServiceProtocol messages such that they can be received by Actor<XPCEchoService>
    public static func _boxXPCEchoServiceProtocol(_ message: GeneratedActor.Messages.XPCEchoServiceProtocol) -> XPCEchoService.Message {
        .xPCEchoServiceProtocol(message)
    } 
    
}
// ==== ----------------------------------------------------------------------------------------------------------------
// MARK: DO NOT EDIT: Generated XPCEchoService behavior

extension XPCEchoService {

    public static func makeBehavior(instance: XPCEchoService) -> Behavior<Message> {
        return .setup { _context in
            let context = Actor<XPCEchoService>.Context(underlying: _context)
            let instance = instance

            instance.preStart(context: context)

            return Behavior<Message>.receiveMessage { message in
                switch message { 
                
                
                case .xPCEchoServiceProtocol(.echo(let string, let _replyTo)):
                    let result = instance.echo(string: string)
                    _replyTo.tell(result)
 
                case .xPCEchoServiceProtocol(.letItCrash):
                    instance.letItCrash()
 
                }
                return .same
            }.receiveSignal { _context, signal in 
                let context = Actor<XPCEchoService>.Context(underlying: _context)

                switch signal {
                case is Signals.PostStop: 
                    instance.postStop(context: context)
                    return .same
                case let terminated as Signals.Terminated:
                    switch try instance.receiveTerminated(context: context, terminated: terminated) {
                    case .unhandled: 
                        return .unhandled
                    case .stop: 
                        return .stop
                    case .ignore: 
                        return .same
                    }
                default:
                    try instance.receiveSignal(context: context, signal: signal)
                    return .same
                }
            }
        }
    }
}
// ==== ----------------------------------------------------------------------------------------------------------------
// MARK: Extend Actor for XPCEchoService

extension Actor where A.Message == XPCEchoService.Message {

}
